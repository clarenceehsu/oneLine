"""
oneline.core is the set of main functions and features, which contains:

    1. io, I/O functions
    2. OneData, extended DataFrame
    3. OneSeries, extended Series
    4. plot(private), a plot function for fast using
"""

from .io import IO
from .onedata import OneData
from .oneseries import OneSeries
